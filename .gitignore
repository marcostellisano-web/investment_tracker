# Python
__pycache__/
.token_cache.json
*.pyc
*.pyo
.venv/
venv/
env/

# Don't accidentally commit a config with real wallet addresses
# (wallets are public, but good hygiene — use config.yaml as a template)
# Remove this line if you want to track your config in git:
# config.yaml
